---
description: "Shorthand for /ignite. Activates max-effort mode."
---

Run `/ignite`. (See `commands/ignite.md` for the full protocol.)

**Skip for trivial tasks** — IGNITE fires 6+ background agents and adds protocol overhead. For one-line fixes, single-file edits, or quick Q&A, just ask normally. Use IGNITE for complex multi-file work, audits, or anything you want done with maximum verification.
